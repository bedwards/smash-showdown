# Game Constants

## Survival Mechanics
```lua
```

## World Constants
```lua
local WORLD_SIZE = 5000 -- MASSIVE world
local BIOMES = {
	ground.Size = Vector3.new(WORLD_SIZE * 2.5, 20, WORLD_SIZE * 2.5)
		local x = (math.random() - 0.5) * WORLD_SIZE * 2
		local z = (math.random() - 0.5) * WORLD_SIZE * 2
		local x = (math.random() - 0.5) * WORLD_SIZE * 2
		local z = (math.random() - 0.5) * WORLD_SIZE * 2
```
